# This enables the modern AndroidX libraries
android.useAndroidX=true
# This automatically converts older libraries to work with AndroidX
android.enableJetifier=true

# Optional: Makes the build slightly faster
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
